<h2>Sign In</h2>

<%= form_tag do |form| %>
  <label>Name</label>
  <%= collection_select :user, :name, @users, :name, :name, prompt: true %>
  <br>

  <label>Password</label>
  <%= password_field_tag "password" %>
  <br>

  <%= submit_tag "Sign In" %>
<% end %>